{
  "name": "com.fx.app",
  "version": "1.0.0",
  "description": "FX — Controle Financeiro",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp -r index.html style.css app.js manifest.json service-worker.js icon-192.png icon-512.png www/ 2>/dev/null || true"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
